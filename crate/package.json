{
  "name": "inflate-wasm-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:wasm": "bash build-wasm.sh",
    "build:wrapper": "tsc",
    "build:bundle": "webpack --config webpack.config.js",
    "build": "pnpm build:wasm && pnpm build:wrapper && pnpm build:bundle"
  },
  "devDependencies": {
    "webpack": "^5.107.2",
    "webpack-cli": "^7.0.2"
  }
}
